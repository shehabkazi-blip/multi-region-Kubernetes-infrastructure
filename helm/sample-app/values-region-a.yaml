# Overrides applied on top of values.yaml when deploying to Region A
region: "us-east-1"

image:
  repository: <ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/sample-app
  tag: "latest"

# Add any region-specific tuning here, e.g. different replica counts,
# node selectors for region-specific node groups, etc.
replicaCount: 2
